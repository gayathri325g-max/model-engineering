# Feature selection: drop SibSp and Parch.
# These are low-signal individually and partly captured by other family proxies.
# fewer columns, same strong predictors, cleaner model.
drop_columns:
  - SibSp
  - Parch
scale_numeric: true
one_hot_encode: true
pca_enabled: false
