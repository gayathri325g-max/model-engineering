model_type: random_forest
# Ensemble of decorrelated trees; probabilities are averaged so already
# somewhat calibrated — calibrate=false is a reasonable default.
calibrate: false
params:
  n_estimators: 200
  max_depth: 8
  min_samples_leaf: 5
  random_state: 42
  n_jobs: -1
