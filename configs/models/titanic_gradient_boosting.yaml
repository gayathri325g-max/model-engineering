model_type: gradient_boosting
# Sequential boosting; probabilities can be over-confident on small datasets.
# calibrate=false keeps the native Friedman-sigmoid outputs for comparison.
calibrate: false
params:
  n_estimators: 200
  learning_rate: 0.09
  max_depth: 4
  min_samples_leaf: 6
  subsample: 0.8
  random_state: 42
